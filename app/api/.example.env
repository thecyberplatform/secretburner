# ----------------------------------------------------------------------------------------------------------------------
#                                           _     _
#                    ___  ___  ___ _ __ ___| |_  | |__  _   _ _ __ _ __   ___ _ __
#                   / __|/ _ \/ __| '__/ _ \ __| | '_ \| | | | '__| '_ \ / _ \ '__|
#                   \__ \  __/ (__| | |  __/ |_  | |_) | |_| | |  | | | |  __/ |
#                   |___/\___|\___|_|  \___|\__| |_.__/ \__,_|_|  |_| |_|\___|_|
#
#                                        Edit below this
#
# ----------------------------------------------------------------------------------------------------------------------

# ----------------------------------------------------------------------------------------------------------------------
# Security:
#
#  SECRET_KEY                : A long random string used for salting the hashes generated by Django. Leaking this
#                              will be a security nightmare for you - so KEEP IT SECURE.
#  USE_PG_CRON               : pg_cron is used to ensure expired secrets don't sit in the database. If you are
#                              deploying on AWS, or self-hosting, you should be able to use this.
#                              If you are deploying to render.com you cannot use this! You need to set this to False.
#  APPLY_PER_VIEW_THROTTLING : Indicate whether you want to throlle access the each endpoint.
#  PER_VIEW_THROTTLE_RATE    : The rate of throttling
#
# ----------------------------------------------------------------------------------------------------------------------
SECRET_KEY=SOMETHINGSuperDuperRandomAndVeryLongProbablyEvenLongerTHanthis
USE_PG_CRON=True
APPLY_PER_VIEW_THROTTLING=False
PER_VIEW_THROTTLE_RATE="20/day"

# ----------------------------------------------------------------------------------------------------------------------
# Debugging:
#
#  DJANGO_DEBUG : Standard Django debugging flag
#  DEBUG_DB     : Turns on SQL query debugging (every single db query will be printed in the logs)
#
#                  ** Make sure both of these flags are set to False in production. **
# ----------------------------------------------------------------------------------------------------------------------
DJANGO_DEBUG=True
DEBUG_DB=False

# ----------------------------------------------------------------------------------------------------------------------
# Application configuration:
#
#  APP_NAME     : The name you are giving your instance of secret burner.
#  API_HOSTNAME : Hostname you will be using for deployment (e.g., secretburner.com)
#  UI_HOSTNAME  : Hostname you will be using for the web UI if different to the API (e.g., app.secretburner.com)
#
#  If you are deploying to render.com, then you will actually need to set these hostnames. The suggested convention is:
#    API_HOSTNAME=api.yourdomain.com
#    UI_HOSTNAME=app.yourdomain.com (or just yourdomain.com)
#    ALLOWED_HOSTS=app.yourdomain.com,yourdomain.com
#
# ----------------------------------------------------------------------------------------------------------------------
APP_NAME="Secret Burner"
API_HOSTNAME=localhost
UI_HOSTNAME=localhost

ALLOWED_HOSTS=*

UI_FULFIL_REQUEST_URI=/fulfil-request/
UI_VIEW_SECRET_URL=/retrieve-secret/

# ----------------------------------------------------------------------------------------------------------------------
# Database configuration:
#
#  POSTGRES_HOST     : Hostname of your PostgreSQL database server.
#  POSTGRES_USER     : The user being authenticated on the server
#  POSTGRES_PASSWORD : Password for the POSTGRES_USER
#  POSTGRES_DBNAME   : The database name to use for secret burner. Django will try to create this database if it
#                      doesn't already exist.
#  POSTGRES_TEST     : The name of the test database to use. Django tests will run on the same server as configured
#                      above.
#
# ----------------------------------------------------------------------------------------------------------------------
POSTGRES_PASSWORD=secretburner
POSTGRES_USER=secretburner
POSTGRES_DBNAME=secretburner
POSTGRES_HOST=secretburner-db
POSTGRES_TEST=test_secretburner

# ----------------------------------------------------------------------------------------------------------------------
# Email configuration:
#
#  ALLOW_EMAIL                       : Whether or not to allow emailing from your secret burner instance.
#  REQUIRE_VERIFICATION              : Does the user have to verify their own email address before the system will
#                                      forward the secret or request?
#  EMAIL_VERIFICATION_EXPIRY_SECONDS : The number of seconds from verification creation, and it expiring.
#  MAILER_ESP                        : The Email Service Provider you want to use. You can use any provider
#                                      supported by AnyMail: https://anymail.dev/en/stable/esps/
#                                      secret burner only supports "sendgrid" at the moment.
#  ESP_API_KEY                       : The API Key for your ESP.
#  MAILER_FROM_EMAIL                 : The email address that you've verified to send from with your ESP.
#  MAILER_REPLY_TO_EMAIL             : (optional) A reply-to email address
#
# ----------------------------------------------------------------------------------------------------------------------
ALLOW_EMAIL=False
EMAIL_VERIFICATION_EXPIRY_SECONDS="90"
REQUIRE_VERIFICATION=True
MAIL_ESP=sendgrid
ESP_API_KEY=
MAILER_FROM_EMAIL=
MAILER_REPLY_TO_EMAIL=

# ----------------------------------------------------------------------------------------------------------------------
# Cache configuration:
#
#  CACHE_BACKEND      : The cache you want to use. The default is redis.
#  CACHE_LOCATION     : URL to the cache server, or the table name if using DatabaseCache.
#  CACHE_CLIENT_CLASS : (optional) if using Redis, you may wish to define the client class to use.
# ----------------------------------------------------------------------------------------------------------------------
CACHE_BACKEND="django_redis.cache.RedisCache"
CACHE_LOCATION="redis://secretburner-cache:6379/1"
CACHE_CLIENT_CLASS="django_redis.client.DefaultClient"

# *****************************************************************************
#                            DO NOT EDIT BELOW THIS LINE
# *****************************************************************************
